FROM node:latest

WORKDIR /app

RUN rm -rf $PWD/* 2>/dev/null
RUN git clone https://github.com/jgarcia900506/insurances-front.git .
RUN npm install && npm run build

EXPOSE 4200

CMD ["npm", "run", "start"]
