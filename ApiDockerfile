FROM gradle:8.12.1-jdk17

WORKDIR /app

RUN rm -rf $PWD/* 2>/dev/null
RUN git clone https://github.com/jgarcia900506/insurances-api.git .
RUN gradle clean build

EXPOSE 8080

CMD ["gradle", "bootRun"]
